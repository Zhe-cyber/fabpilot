# FabPilot local demo broker.
# Anonymous + plaintext is acceptable ONLY because docker-compose binds the port
# to 127.0.0.1 (not the network). Authentication + TLS is the T4 hardening step
# tracked in docs/THREAT_MODEL.md — do not expose this broker as-is.
listener 1883
allow_anonymous true
